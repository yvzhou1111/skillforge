node_modules/
dist/
*.log
.DS_Store
.env
coverage/
.skillforge-cache/
*.skillforge-backup

# Workflow files require the `workflow` OAuth scope to push.
# Templates live in docs/github-workflows/ — see that folder's README to enable.
.github/workflows/
